# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [0.1.4](https://github.com/simonrw/listprojects/releases/tag/v0.1.4) - 2026-03-09

### Fixed

- *(session)* check tmux status when creating session

### Other

- Disable crates.io publishing in release-plz
- Add release-plz integration
- Bump version
- Switch spawn for status
- package update
- Bump version
- Implement --list mode
- Add claude.md
- Add --clear flag to clear the cache
- Bump version
- Only emit new item if not already cached
- Implement disk caching
- Update edition to 2024
- update package versions
- Fix clippy lint
- Include version flag
- Detect system colour theme
- Add mise config
- Set up rust flake
- Fix session name
- Speed up searching
- Implement tmux integration
- Fresh start
- Use tagged version of dependency
- Use my fork of skim
- Update packages
- Pick up skim colour from environment
- Replicate my default settings
- Support performant release builds
- Do not select if abort
- Rename binary
- Remove old Go code
- Finish last few tmux methods
- Fix bug with project list
- Remove debug statement
- Use correct cache syntax
- Start to set up tmux wrapper
- Set up basic working example
- Add rust project
- Handle session already existing
- Update dependencies
- Correct pre-commit hook
- Correct ability to use attach-client
- Correct attach-session arguments
- Extract runTmuxCommand function
- Extract tmux package
- Handle error running tmux
- Save cache on ctrl-c
- Exit with 1 status code on ctrl-c
- Handle ctrl-c
- Implement tmux commands
- Allow prefix and session name configuration
- Read in config file
- Reinstates previous functionality
- Adds golang pre-commit
- Start to set up tmux abstraction
- Add header
- Rename project
- Adds clear option
- Set up go project
- Remove old rust version
- Switch to 2021 edition
- Support custom prefix for directories
- Allow for deeper directory trees
- Only propose paths if they are git directories
- Use environment variable for filtering
- Attach session when not in tmux
- Downgrade two log entries
- Fixes clippy lints
- Refactor out create_builder function
- Adds colour options
- Revert "Shows preview"
- Shows preview
- Supports multiple directories and custom depths
- Use config dir for macos
- Create session if absent
- Do not select on ctrl-c
- Supports config file
- Use full height
- Supports selecting a project
- Supports selecting a session
- Adds toml
- Play with skim and channels
- Adds options parsing
- Includes skim library
- Creates rust project
- Initial commit
